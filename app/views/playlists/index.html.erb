<h1>Playlists de <%= @usuario.nome %></h1>

<% @playlists.each do |playlist| %>
  <h2><%= link_to playlist.nome, usuario_playlist_path(@usuario, playlist) %></h2>
  <p><%= playlist.descricao %></p>
<% end %>
    